{
  "name": "reposage-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix reposage run dev",
    "build": "npm --prefix reposage run build",
    "start": "npm --prefix reposage run start",
    "lint": "npm --prefix reposage run lint",
    "db:generate": "npm --prefix reposage run db:generate",
    "db:migrate": "npm --prefix reposage run db:migrate",
    "db:studio": "npm --prefix reposage run db:studio"
  }
}
